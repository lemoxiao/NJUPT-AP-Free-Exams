.486
DATA SEGMENT USE16
		NUM DW 1001H
DATA ENDS

CODE SEGMENT USE16
ASSUME CS:CODE,DS:DATA
BEG:	
		MOV AX,DATA
		MOV DS,AX
		MOV CX,4
		MOV AX,0
		MOV DX,NUM
JAA:	
		ROL DX,4
		MOV BX,DX
		AND BX,000FH
		IMUL AX,10
		ADD AX,BX
		LOOP JAA
		CALL DISP
		MOV AH,4CH
		INT 21H		


DISP PROC
		MOV CX,16
		MOV BX,AX
LAST1:	MOV AL,'0'
		ROL BX,1
		JNC NEXT
		MOV AL,'1'
NEXT:	MOV AH,0EH
		INT 10H
		LOOP LAST1
		RET
DISP ENDP


		
CODE ENDS

END BEG













